{
  "name": "quant-finance-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Fastify API serving investment projections computed from Parquet market data in Cloudflare R2.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@fastify/cors": "^10.0.1",
    "dotenv": "^16.4.5",
    "fastify": "^5.1.0",
    "hyparquet": "^1.6.0"
  }
}
